

my objects and arrays might look something like this

{
  userData: {
    name: "Cate",
    grandtotal: 24,
    days: [
      {
        day: 1,
        nutrition: "5",
        workout: 1,
        stretch: 0, // just true / false, or the points?
        supp: 1,
        water: 1,
        other: 1,
        comments: 1,
        commenttext: "that was easy",
        dailytotal: 11,
      },
      {
        day: 2,
        nutrition: "5",
        workout: 1,
        stretch: 1, 
        supp: 1,
        water: 1,
        other: 1,
        comments: 1,
        commenttext: "that was hard",
        dailytotal: 13,
      }
    ]
  }
}


to make things more DRY
function updateScoreboard
target and value

MVC - model view controller
firebase

similar to chat client homework in a way
usernames 

start by hardcoding username - always Cate
on submit
gather data
display data (view part)
save data


Notes with David and Kyle Wed night
push the object to firebase (like chat exercise) on submit
after the push, call a fuction to update the table with the scores updateScore ();
function updateScore () { 
    
}

calculate grandTotal on the fly each time
add a row for each new object each time 
also clean up CSS of date picker (done)

my firebase data
https://fcnk5qyiep7.firebaseio-demo.com/

Writing to Firebase - example, not my url
  <script>
      var myDataRef = new Firebase('https://fcnk5qyiep7.firebaseio-demo.com/');
      $('#messageInput').keypress(function (e) {
        if (e.keyCode == 13) {
          var name = $('#nameInput').val();
          var text = $('#messageInput').val();
          myDataRef.push({name: name, text: text}); ***********
          $('#messageInput').val('');
        }
      });
    </script>


    Call back - reading

    myDataRef.on('child_added', function(snapshot) {
  //We'll fill this in later.
});


the whole firebase example (in Hmtl but my script will be in js file)

<body>
    <div id='messagesDiv'></div>
    <input type='text' id='nameInput' placeholder='Name'>
    <input type='text' id='messageInput' placeholder='Message'>
    <script>
      var myDataRef = new Firebase('https://fcnk5qyiep7.firebaseio-demo.com/');
      $('#messageInput').keypress(function (e) {
        if (e.keyCode == 13) {
          var name = $('#nameInput').val();
          var text = $('#messageInput').val();
          myDataRef.push({name: name, text: text});
          $('#messageInput').val('');
        }
      });
      myDataRef.on('child_added', function(snapshot) {
        var message = snapshot.val();
        displayChatMessage(message.name, message.text);
      });
      function displayChatMessage(name, text) {
        $('<div/>').text(text).prepend($('<em/>').text(name+': ')).appendTo($('#messagesDiv'));
        $('#messagesDiv')[0].scrollTop = $('#messagesDiv')[0].scrollHeight;
      };
    </script>
  </body>


  put start over button in <div> from the beginning and display none CSS
  then .show the div when over 28 rows

  // fixing daily average - function and variable inside can't be the same name
